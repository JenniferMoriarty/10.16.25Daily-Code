//instructions: change this loop so it prints out the numbers 4-16 counting by 2s
#include <iostream>
using namespace std;

int main() {

    for (int i = 1; i <= 5; i++) { //remember "i++" can be written as "i+=1"
        cout << i << " ";
    } //technically the {} aren't needed unless you have more than one line, but it's good coding practice to include them!

}
